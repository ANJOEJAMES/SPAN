// Global Configuration file
// When deploying the frontend to Vercel and running the backend locally, 
// replace the below URL with your specific localtunnel URL (e.g., 'https://your-tunnel-url.loca.lt/api')
window.SPAN_API_URL = 'http://localhost:3000/api';
